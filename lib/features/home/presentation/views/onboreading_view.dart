import 'package:flutter/material.dart';

class OnboreadingView extends StatelessWidget {
  const OnboreadingView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('OnboreadingView1')));
  }
}

class OnboreadingView2 extends StatelessWidget {
  const OnboreadingView2({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('OnboreadingView2')));
  }
}

class OnboreadingView3 extends StatelessWidget {
  const OnboreadingView3({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('OnboreadingView3')));
  }
}
