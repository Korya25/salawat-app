class AppConstants {
  static const String appFontFamily = 'Tajawal';

  // Animation durations
  static const Duration pageTransitionDuration = Duration(milliseconds: 500);
  static const Duration buttonAnimationDuration = Duration(milliseconds: 300);
  static const Duration fadeAnimationDuration = Duration(milliseconds: 800);

  // Padding and margins
  static const double horizontalPadding = 24.0;
  static const double verticalPadding = 16.0;
  static const double iconPadding = 20.0;
  static const double subtitleHorizontalPadding = 16.0;

  // Sizes
  static const double buttonHeight = 56.0;
  static const double iconSize = 60.0;
  static const double pageIndicatorSize = 8.0;
  static const double activePageIndicatorWidth = 24.0;
  static const double pageIndicatorBorderRadius = 4.0;
  static const double buttonBorderRadius = 12.0;

  // Opacities
  static const double gradientStartOpacity = 0.1;
  static const double shadowOpacity = 0.3;
  static const double textOpacity = 0.8;
  static const double inactiveIndicatorOpacity = 0.4;

  // Spacing
  static const double smallSpacing = 8.0;
  static const double mediumSpacing = 20.0;
  static const double largeSpacing = 40.0;
}
